# Facebook OAuth Configuration (used for both Facebook and Instagram via Business Login)
FB_CLIENT_ID=
FB_CLIENT_SECRET=
FB_REDIRECT_URI=http://localhost:5000/auth/facebook/callback

# Instagram Configuration (optional - uses Facebook credentials via Business Login)
INSTA_REDIRECT_URI=http://localhost:5000/auth/instagram/callback

# Additional configuration (optional)
DEBUG_MODE=False
API_VERSION=v12.0